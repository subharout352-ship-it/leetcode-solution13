class Solution(object):
    def rob(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        prev = 0
        curr = 0

        for money in nums:
            prev, curr = curr, max(curr, prev + money)

        return curr
